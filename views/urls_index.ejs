<!DOCTYPE html>
<html lang="en">
  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp</title>
    <style>
      td{
        padding: 5px 10px 5px 10px;
      }
    </style>

  </head>

  <body style="font-size:20px; padding:50px;">

    <% include partials/_header %>

    <h2> My URLs </h2>
    <table> <% for (var shortenedURL in urls) { %>
      <tr style="padding-bottom:15px">
        <td><b> <%= shortenedURL%> </b> </td>
        <td> <%= urls[shortenedURL] %></td>
        <td> <a href="/urls/<%=shortenedURL%>" >Edit</a> </td>
        <td> <form method="POST" action="/urls/<%= shortenedURL %>/delete" id="deleteform" style="display:inline">
              <button type="submit" class="btn btn-primary">Delete Link</button>
          </form> </td>
        </tr>
     <% } %>
    </table>

  </body>
</html>